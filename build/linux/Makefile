# -*- coding: utf-8-unix -*-
# vim:fileencoding=utf-8:ff=unix

# ----------------------------------------------------------

PREFIX         :=
CXX            := $(PREFIX)$(CXX)
AR             := $(PREFIX)$(AR)
RANLIB         := $(PREFIX)ranlib

CXXFLAGS        =
CPPFLAGS        =
ARFLAGS         = rcSU

WARNADD        :=

# ----------------------------------------------------------

include ../unix-common/common.mk
